---
title: Flare-On 12-3 Write-Up
date: 2026-04-17 12:00:00 -0400
categories: [CTF, Flare-On 12]
tags: [PDF]
excerpt: >
    A malformed PDF hiding an embedded JPG image.
---

### 1.Analyzing the PDF


Although the PDF should work with PDFjs (Firefox), Librewolf fails to render it because it is malformed.

```
% Tested under the following browsers:
% Chrome, Safari, PDFjs (Firefox)
```

Adding `endobj` to objects 4 and 7 fixes the issue (likely intentionally removed).

![PDF](/assets/img/posts/flare12/PDF.png)

From the PDF specification (ISO 32000-2:2020):

> Encryption-related information shall be stored in a document’s encryption dictionary, which shall be
the value of the Encrypt entry in the document’s trailer dictionary

```
trailer <<
  /Root 2 0 R
  /#52#6F#6F#74 1
  % /Size 15
  0
  R
  /Encrypt 7 0 R
>>
```

Encryption applies to all strings and streams in the document's PDF file, with some exceptions.
This applies to object 4 as well, which holds the actual contents of the PDF we care about
(which are also zlib-compressed).

```
4 0 obj
<</Length 320 /Filter /FlateDecode>>
stream
±x¸l ÝÄXY ...
endstream
endobj
```

Since our browser rendered the contents of the stream ("Flare-On!"), it must have derived a valid FEK (file encryption key) for the PDF.
As we didn't supply any user input, it's possible it tried to derive the key from an empty password, which was indeed
the case in this situation.

### 2. Solution

After decompressing the contents, we're left with a stream of data with an embedded jpg image, the grayscale values of all
the pixels form a string, which is the flag.

1. qpdf --decrypt pretty_devilish_file.pdf decrypted.pdf
2. pdfimages -all decrypted.pdf flag
3. magick flag-000.jpg -colorspace Gray -depth 8 gray:gray.raw
4. strings gray.raw

The final command outputs

```
Puzzl1ng-D3vilish-F0rmat@flare-on.com
```

### 3. (Extra) Deriving the FEK by hand

This section is optional reading which goes over encryption in PDFs, and how we could have
discovered the flag on our own (without tools).

#### 3.1 Understanding the encryption dictionary (object 7)

As mentioned previously, object 7 holds all encryption-related information.
It looks like this (with clearer formatting)

```
7 0 obj
<<
    /Filter /Standard
    /V 5
    /R 6
    /P -1
    /U (ümþßôµèäÂHÒ¼ø¶kë¼ÚÍc BT6fÒÅ¤ÃTYMñ j12Jw)
    /O (v/Î^ 3wéWXÆðÛ3ÆÜ0¤~A[ÜwvutmLl9W`AiòX«)
    /UE ( Ò3gûe} ûJÄVwb¶ûÐr³WHa}?ë)
    /OE (Ý\\zRõC8Ã©í¹Ëú¨gEòðmÂRÌÊõ)
    /Perms (G²KDÇ½ µùHe´â)
    /CF
    <<
        /StdCF
        <<
            /AuthEvent /DocOpen
            /CFM /AESV3
            /Length 32
        >>
    >>
    /StrF /StdCF
    /StmF /StdCF
    /Length 256
    /EncryptMetadata true
>>
endobj
```

`/Filter /Standard` specifies we use the password-based encryption system (standard security handler)
defined in the specification. 

`/V` specifies the algorithm to be used in encrypting and decrypting the document. Algorithms are defined in
the specification and can be found in Appendix A. When the value of `/V` in the encryption dictionary is 5,
(only valid case in PDF 2.0) then algorithm 1.A is used, otherwise for 1, 2, 3, and 4, algorithm 1 is used.

`/R` specifies which revision of the standard security handler is used to interpret the dictionary. In other words,
it determines rules for key interpretation, such as which keys are required under certain revisions and what they mean.

`/P` is a set of flags specifying which operations are permitted when the document is opened with user access.

PDF’s standard security handler allows access permissions and up to two passwords to be specified for a document:
an owner password and a user password. In certain compliant viewers, they will enforce the permissions specified by P
if you user the user password, and ignore P if you used the owner password.

`/U` is a hash derived from the user password
`/O` is a hash derived from the owner password
`/UE` is the FEK encrypted with a hash derived from the user password
`/UO` is the FEK encrypted with a hash derived from the owner password

The algorithms used in revision 6 to compute these values can be found in Appendix B (alongside the hashing algorithm 
algorithm that many of them reference).

Crypt filters are defined in `/CF` and allow for finer control over encryption within the PDF file. In our case, we define
the `/StdCF` filter and specify to use it to decrypt all the streams (StmF) and strings (StrF) in the document.

In our crypt filter, we specify
- `/AuthEvent /DocOpen` which means we enforce encryption when the document is opened (you must authenticate before viewing the doc).
- `/CFM /AESV3` means we decrypt data using the AES-256 algorithm in Cipher Block Chaining (CBC) with padding mode with a 16-byte block size
- `/Length 32` is deprecated in PDF 2.0, again just the length of the FEK in bytes.

`/Length` determines the length of the FEK in bits, but it's deprecated in PDF 2.0 (only valid if `/V` is 2 or 3).
`/EncryptMetadata` indicates whether the document-level metadata stream gets encrypted.


#### 3.2 Getting the FEK with python

Everything we need to know is written in the specification which you can get from the [official source](https://pdfa.org/resource/pdf-specification-archive/) or
other places (caugh [library genesis](https://librarygenesis.net/) caugh).



##### 3.2.1 Parsing string literals

First we need to parse the `/U`, `/O`, `/UE`, and `/UO` values, which are string literals.
String literals in PDFs can support nested parentheses, so we can't simply use the `\([^)]*\)` regex
pattern. We need to find balanced parentheses manually. Once we've found all the bytes, we'll then
need to account for escape sequences.

These are the following escape sequences supported in PDF string literals:

| Sequence        | Meaning    |
|--------------- | --------------- |
| \n  | LINE FEED (0Ah) (LF   |
| \r  | CARRIAGE RETURN (0Dh) (CR)   |
| \t  | HORIZONTAL TAB (09h) (HT)|
| \b  | BACKSPACE (08h) (BS)|
| \f  | FORM FEED (0Ch) (FF) |
| \\(  | LEFT PARENTHESIS (28h) |
| \\)  | RIGHT PARENTHESIS (29h) |
| \\\  | REVERSE SOLIDUS (5Ch) (Backslash) |
| \ddd  | Character code ddd (octal) |


```py
def parse_pdf_literal_string(data: bytes, key: bytes) -> bytes:
    SIMPLE_ESCAPES = {
        b"n": b"\x0A",
        b"r": b"\x0D",
        b"t": b"\x09",
        b"b": b"\x08",
        b"f": b"\x0C",
        b"\\": b"\x5C",
        b"(": b"\x28",
        b")": b"\x29",
    }

    ESCAPE_RE = re.compile(
        rb"\\(?:"
        rb"([nrtbf\\()])"
        rb"|([0-7]{1,3})"
        rb"|\r\n?|\n"
        rb")"
    )

    pattern = re.escape(key) + rb"\s*\("
    match = re.search(pattern, data)
    if not match:
        raise ValueError(f"Key {key} not found in PDF")

    depth = 1
    start = match.end()
    i = start

    while i < len(data) and depth > 0:
        b = data[i]
        if b == 0x5C:
            i += 2
            continue
        elif b == 0x28:
            depth += 1
        elif b == 0x29:
            depth -= 1
        i += 1

    content = data[start:i-1]

    # Handle PDF string literal escape sequences
    def replace(m: re.Match) -> bytes:
        if g := m.group(1):
            return SIMPLE_ESCAPES[g]
        if g := m.group(2):
            return bytes([int(g, 8) & 0xFF])
        return b""  # line continuation

    return ESCAPE_RE.sub(replace, content)


def get_encryption_values(data: bytes):
    U = parse_pdf_literal_string(data, b"/U")
    O = parse_pdf_literal_string(data, b"/O")
    UE = parse_pdf_literal_string(data, b"/UE")
    OE = parse_pdf_literal_string(data, b"/OE")

    assert len(U) == 48, f"U  should be 48 bytes, got {len(U)}"
    assert len(O) == 48, f"O  should be 48 bytes, got {len(O)}"
    assert len(UE) == 32, f"UE should be 32 bytes, got {len(UE)}"
    assert len(OE) == 32, f"OE should be 32 bytes, got {len(OE)}"

    return U, O, UE, OE
```

##### 3.2.2 Implementing the hashing algorithm used in revision 6

This hashing algorithm is used in pretty much every password algorithm relevant
to us.

```
Take the SHA-256 hash of the original input to the algorithm and name the resulting 32 bytes, K.
Perform the following steps (a)-(d) 64 times:

a) Make a new string, K1, consisting of 64 repetitions of the sequence: input password, K, the 48-byte user
key. The 48 byte user key is only used when checking the owner password or creating the owner key. If
checking the user password or creating the user key, K1 is the concatenation of the input password and K.

b) Encrypt K1 with the AES-128 (CBC, no padding) algorithm, using the first 16 bytes of K as the key and
the second 16 bytes of K as the initialization vector. The result of this encryption is E.

c) Taking the first 16 bytes of E as an unsigned big-endian integer, compute the remainder, modulo 3. If the
result is 0, the next hash used is SHA-256, if the result is 1, the next hash used is SHA-384, if the result is 2,
the next hash used is SHA-512.

d) Using the hash algorithm determined in step c, take the hash of E. The result is a new value of K, which
will be 32, 48, or 64 bytes in length.

Repeat the process (a-d) with this new value for K. Following 64 rounds (round number 0 to round
number 63), do the following, starting with round number 64:

NOTE 2:  The reason for multiple rounds is to defeat the possibility of running all paths in parallel. With 64
         rounds (minimum) there are 3^64 paths through the algorithm.

e) Look at the very last byte of E. If the value of that byte (taken as an unsigned integer) is greater than the
round number - 32, repeat steps (a-d) again.

f) Repeat from steps (a-e) until the value of the last byte is ≤ (round number) - 32.

NOTE 3:  Tests indicate that the total number of rounds will most likely be between 65 and 80.
         The first 32 bytes of the final K are the output of the algorithm.
```

```py
def pdf_r6_hash(password: bytes, salt: bytes, U: bytes = b"") -> bytes:
    """
    Algorithm 2.B: Computing a hash (revision 6 and later)
    """

    def aes_128_cbc_encrypt(key: bytes, iv: bytes, pt: bytes) -> bytes:
        cipher = AES.new(key, AES.MODE_CBC, iv)
        return cipher.encrypt(pt)

    k = hashlib.sha256(password + salt + U).digest()
    round_num = 0

    while True:
        # Step (a): Compute K
        if U:
            seq = password + k + U
        else:
            seq = password + k

        k1 = seq*64

        # Step (b): Encrypt K1
        e = aes_128_cbc_encrypt(k[:16], k[16:32], k1)

        # Step (c): Determine the hashing alg to use
        remainder = int.from_bytes(e[:16], byteorder='big') % 3

        match remainder:
            case 0:
                hash_func = hashlib.sha256
            case 1:
                hash_func = hashlib.sha384
            case 2:
                hash_func = hashlib.sha512

        # Step (d): Hash E
        k = hash_func(e).digest()

        round_num += 1

        # Steps (e-f): After 64 rounds, check the last byte of E
        if (round_num >= 64):
            if (e[-1] <= (round_num - 32)):
                break

    return k[:32]
```

We can confirm that the empty passwords were in fact correct, you can view the algorithms
to authenticate both the user and owner passwords in Appendix B (11 and 12).

```py
def main():
    data = read_file("pretty_devilish_file.pdf")
    U, O, UE, OE = get_encryption_values(data)

    U_HASH = U[:32]
    U_VALIDATION_SALT = U[32:40]
    U_KEY_SALT = U[40:48]

    O_HASH = O[:32]
    O_VALIDATION_SALT = O[32:40]
    O_KEY_SALT = O[40:48]

    password = b""
    computed_hash = pdf_r6_hash(password, U_VALIDATION_SALT)
    if (U_HASH == computed_hash):
        print("Correct user password")
    else:
        print("Wrong user password")

    computed_hash = pdf_r6_hash(password, O_VALIDATION_SALT, U)
    if (O_HASH == computed_hash):
        print("Correct owner password")
    else:
        print("Wrong owner password")


if __name__ == "__main__":
    main()
```

##### 3.2.3 Decrypting UE and OE

Finally we can derive the FEK by simply decrypting either UE or OE, you'll
see they both lead to the same result

```py
def get_fek(ct: bytes, password: bytes, key_salt: bytes, U: bytes = b"") -> bytes:
    k = pdf_r6_hash(password, key_salt, U)
    cipher = AES.new(k, AES.MODE_CBC, b'\x00'*0x10)
    return cipher.decrypt(ct)


def main():

    ...

    fek = get_fek(UE, password, U_KEY_SALT)
    print(fek.hex(' '))

    fek = get_fek(OE, password, O_KEY_SALT, U)
    print(fek.hex(' '))
```

#### 3.3 Getting the flag

As per the specification:

> Use the AES algorithm in Cipher Block Chaining (CBC) mode, which requires an initialization
vector. The block size parameter is set to 16 bytes, and the initialization vector is a 16-byte random
number that is stored as the first 16 bytes of the encrypted stream or string.

```py
def get_encrypted_stream(data):
    stream_size = 320
    pos = data.find(b"stream\r\n") + len("stream\r\n")
    return data[pos:pos+stream_size]

def main():

    ...

    encrypted_stream = get_encrypted_stream(data)

    cipher = AES.new(fek, AES.MODE_CBC, encrypted_stream[:16])
    decrypted_stream = cipher.decrypt(encrypted_stream[16:])

    raw_stream = zlib.decompress(decrypted_stream)
    print(raw_stream)
```

The output gives us:

```
b"q 612 0 0 10 0 -10 cm\nBI /W 37/H 1/CS/G/BPC 8/L 458/F[\n/AHx\n/DCT\n]ID\\nEI Q \n\nq\nBT\n/ 140 Tf\n10 10 Td\n(Flare-On!)'\nET\nQ\n"
```

If you didn't recognize the `FFD8` JPG header, section 8.9.7 on inline images in the specification gives it away:

> An inline image object shall be delimited in the content stream by the operators BI (begin image), ID
(image data), and EI (end image). These operators are summarised in "Table 90 — Inline image
operators". BI and ID shall bracket a series of key-value pairs specifying the characteristics of the
image, such as its dimensions and colour space; the image data shall follow between the ID and EI
operators. The format is thus analogous to that of a stream object such as an image XObject:

```
BI
...Key-value pairs...
ID
...Image data...
EI
```

So all that's left is to extract the data and let `pillow` do the rest.

```py
def extract_jpeg(data: bytes) -> bytes:
    start = data.find(b"ID")
    start += 2
    end = data.find(b"EI", start)
    hex_data = b"".join(data[start:end].split())
    return bytes.fromhex(hex_data)

def main():

    ...

    jpg_data = extract_jpeg(raw_stream)
    img = Image.open(BytesIO(jpg_data)).convert("L")  # grayscale
    pixels = img.get_flattened_data()
    flag = ''.join(map(chr, pixels))
    print(flag)
```

#### 3.4 Full Script

```py
#!/usr/bin/env python

import re
import hashlib
from Crypto.Cipher import AES
import zlib
from PIL import Image
from io import BytesIO


def read_file(file_name):
    with open(file_name, "rb") as f:
        return f.read()


def parse_pdf_literal_string(data: bytes, key: bytes) -> bytes:
    SIMPLE_ESCAPES = {
        b"n": b"\x0A",
        b"r": b"\x0D",
        b"t": b"\x09",
        b"b": b"\x08",
        b"f": b"\x0C",
        b"\\": b"\x5C",
        b"(": b"\x28",
        b")": b"\x29",
    }

    ESCAPE_RE = re.compile(
        rb"\\(?:"
        rb"([nrtbf\\()])"
        rb"|([0-7]{1,3})"
        rb"|\r\n?|\n"
        rb")"
    )

    pattern = re.escape(key) + rb"\s*\("
    match = re.search(pattern, data)
    if not match:
        raise ValueError(f"Key {key} not found in PDF")

    depth = 1
    start = match.end()
    i = start

    while i < len(data) and depth > 0:
        b = data[i]
        if b == 0x5C:
            i += 2
            continue
        elif b == 0x28:
            depth += 1
        elif b == 0x29:
            depth -= 1
        i += 1

    content = data[start:i-1]

    # Handle PDF string literal escape sequences
    def replace(m: re.Match) -> bytes:
        if g := m.group(1):
            return SIMPLE_ESCAPES[g]
        if g := m.group(2):
            return bytes([int(g, 8) & 0xFF])
        return b""  # line continuation

    return ESCAPE_RE.sub(replace, content)


def get_encryption_values(data: bytes):
    U = parse_pdf_literal_string(data, b"/U")
    O = parse_pdf_literal_string(data, b"/O")
    UE = parse_pdf_literal_string(data, b"/UE")
    OE = parse_pdf_literal_string(data, b"/OE")

    assert len(U) == 48, f"U  should be 48 bytes, got {len(U)}"
    assert len(O) == 48, f"O  should be 48 bytes, got {len(O)}"
    assert len(UE) == 32, f"UE should be 32 bytes, got {len(UE)}"
    assert len(OE) == 32, f"OE should be 32 bytes, got {len(OE)}"

    return U, O, UE, OE


def pdf_r6_hash(password: bytes, salt: bytes, U: bytes = b"") -> bytes:
    """
    Algorithm 2.B: Computing a hash (revision 6 and later)
    """

    def aes_128_cbc_encrypt(key: bytes, iv: bytes, pt: bytes) -> bytes:
        cipher = AES.new(key, AES.MODE_CBC, iv)
        return cipher.encrypt(pt)

    k = hashlib.sha256(password + salt + U).digest()
    round_num = 0

    while True:
        # Step (a): Compute K
        if U:
            seq = password + k + U
        else:
            seq = password + k

        k1 = seq*64

        # Step (b): Encrypt K1
        e = aes_128_cbc_encrypt(k[:16], k[16:32], k1)

        # Step (c): Determine the hashing alg to use
        remainder = int.from_bytes(e[:16], byteorder='big') % 3

        match remainder:
            case 0:
                hash_func = hashlib.sha256
            case 1:
                hash_func = hashlib.sha384
            case 2:
                hash_func = hashlib.sha512

        # Step (d): Hash E
        k = hash_func(e).digest()

        round_num += 1

        # Steps (e-f): After 64 rounds, check the last byte of E
        if (round_num >= 64):
            if (e[-1] <= (round_num - 32)):
                break

    return k[:32]


def get_fek(ct: bytes, password: bytes, key_salt: bytes, U: bytes = b"") -> bytes:
    k = pdf_r6_hash(password, key_salt, U)
    cipher = AES.new(k, AES.MODE_CBC, b'\x00'*0x10)
    return cipher.decrypt(ct)


def get_encrypted_stream(data):
    stream_size = 320
    pos = data.find(b"stream\r\n") + len("stream\r\n")
    return data[pos:pos+stream_size]


def extract_jpeg(data: bytes) -> bytes:
    start = data.find(b"ID")
    start += 2
    end = data.find(b"EI", start)
    hex_data = b"".join(data[start:end].split())
    return bytes.fromhex(hex_data)


def main():
    data = read_file("pretty_devilish_file.pdf")
    U, O, UE, OE = get_encryption_values(data)

    U_HASH = U[:32]
    U_VALIDATION_SALT = U[32:40]
    U_KEY_SALT = U[40:48]

    O_HASH = O[:32]
    O_VALIDATION_SALT = O[32:40]
    O_KEY_SALT = O[40:48]

    password = b""
    computed_hash = pdf_r6_hash(password, U_VALIDATION_SALT)
    if not (U_HASH == computed_hash):
        print("Wrong user password")

    computed_hash = pdf_r6_hash(password, O_VALIDATION_SALT, U)
    if not (O_HASH == computed_hash):
        print("Wrong owner password")

    fek = get_fek(UE, password, U_KEY_SALT)
    fek = get_fek(OE, password, O_KEY_SALT, U)

    encrypted_stream = get_encrypted_stream(data)
    cipher = AES.new(fek, AES.MODE_CBC, encrypted_stream[:16])
    decrypted_stream = cipher.decrypt(encrypted_stream[16:])
    raw_stream = zlib.decompress(decrypted_stream)

    jpg_data = extract_jpeg(raw_stream)
    img = Image.open(BytesIO(jpg_data)).convert("L")  # grayscale
    pixels = img.get_flattened_data()
    flag = ''.join(map(chr, pixels))
    print(flag)


if __name__ == "__main__":
    main()
```


### Appendix A

#### 7.6.3.2  Algorithm 1: Encryption of data using the RC4 or AES algorithms

This algorithm is deprecated in PDF 2.0.

a) Obtain the object number and generation number from the object identifier of the string or stream to be
encrypted (see 7.3.10, "Indirect objects"). If the string is a direct object, use the identifier of the indirect
object containing it.

b) For all strings and streams without crypt filter specifier; treating the object number and generation
number as binary integers, extend the original n-byte file encryption key to n + 5 bytes by appending the
low-order 3 bytes of the object number and the low-order 2 bytes of the generation number in that
order, low-order byte first. (n is 5 unless the value of V in the encryption dictionary is greater than 1, in
which case n is the value of Length divided by 8.) For example, for object number 258 and generation
number 7, the hexadecimal values 0x02 0x01 0x00 0x07 0x00 would be appended to the file encryption
key.

If using the AES algorithm, extend the file encryption key an additional 4 bytes by adding the value
"sAlT", which corresponds to the hexadecimal values 0x73, 0x41, 0x6C, 0x54. (This addition is done
for backward compatibility and is not intended to provide additional security.)

c) Initialise the MD5 hash function and pass the result of step (b) as input to this function.

d) Use the first (n + 5) bytes, up to a maximum of 16, of the output from the MD5 hash as the key for the
RC4 or AES symmetric key algorithms, along with the string or stream data to be encrypted.
If using the AES algorithm, the Cipher Block Chaining (CBC) mode, which requires an initialization
vector, is used. The block size parameter is set to 16 bytes, and the initialization vector is a 16-byte
random number that is stored as the first 16 bytes of the encrypted stream or string.
The output is the encrypted data to be stored in the PDF file.

#### 7.6.3.3  Algorithm 1.A: Encryption of data using the AES algorithms

a) Use the 32-byte file encryption key for the AES-256 symmetric key algorithm, along with the string or
stream data to be encrypted. Use the AES algorithm in Cipher Block Chaining (CBC) mode, which requires an
initialization vector. The block size parameter is set to 16 bytes, and the initialization vector is a 16-byte
random number that is stored as the first 16 bytes of the encrypted stream or string. The output is the encrypted
data to be stored in the PDF file.

Stream data shall be encrypted after applying all stream encoding filters and shall be decrypted before
applying any stream decoding filters. The number of bytes to be encrypted or decrypted shall be given
by the Length entry in the stream dictionary. Decryption of strings (other than those in the encryption
dictionary) shall be done after escape-sequence processing and hexadecimal decoding as appropriate
to the string representation described in 7.3.4, "String objects".

### Appendix B

#### 7.6.4.4.7  Algorithm 8: Computing the encryption dictionary’s U (user password) and UE (user encryption) values (Security handlers of revision 6)

a) Generate 16 random bytes of data using a strong random number generator. The first 8 bytes are the
User Validation Salt. The second 8 bytes are the User Key Salt. Compute the 32-byte hash using algorithm
2.B with an input string consisting of the UTF-8 password concatenated with the User Validation Salt.
The 48- byte string consisting of the 32-byte hash followed by the User Validation Salt followed by the
User Key Salt is stored as the U key.
b) Compute the 32-byte hash using algorithm 2.B with an input string consisting of the UTF-8 password
concatenated with the User Key Salt. Using this hash as the key, encrypt the file encryption key using
AES-256 in CBC mode with no padding and an initialization vector of zero. The resulting 32-byte string is
stored as the UE key.

#### 7.6.4.4.8  Algorithm 9: Computing the encryption dictionary’s O (owner password) and OE (owner encryption) values (Security handlers of revision 6)

a) Generate 16 random bytes of data using a strong random number generator. The first 8 bytes are the
Owner Validation Salt. The second 8 bytes are the Owner Key Salt. Compute the 32-byte hash using
algorithm 2.B with an input string consisting of the UTF-8 password concatenated with the Owner
Validation Salt and then concatenated with the 48-byte U string as generated in Algorithm 8. The 48-byte
string consisting of the 32-byte hash followed by the Owner Validation Salt followed by the Owner Key
Salt is stored as the O key.
b) Compute the 32-byte hash using 7.6.4.3.4, "Algorithm 2.B: Computing a hash (revision 6 and later)" with
an input string consisting of the UTF-8 password concatenated with the Owner Key Salt and then
concatenated with the 48-byte U string as generated in 7.6.4.4.7, "Algorithm 8: Computing the
encryption dictionary’s U (user password) and UE (user encryption) values (Security handlers of
revision 6)". Using this hash as the key, encrypt the file encryption key using AES-256 in CBC mode with
no padding and an initialization vector of zero. The resulting 32-byte string is stored as the OE key.

#### 7.6.4.4.9  Algorithm 10: Computing the encryption dictionary’s Perms (permissions) value (Security handlers of revision 6)

Fill a 16-byte block as follows:
a) Extend the permissions (contents of the P integer) to 64 bits by setting the upper 32 bits to all 1’s.
NOTE This allows for future extension without changing the format.
b) Record the 8 bytes of permission in the bytes 0-7 of the block, low order byte first.
c) Set byte 8 to the ASCII character "T" or "F" according to the EncryptMetadata boolean.
d) Set bytes 9-11 to the ASCII characters '"a", "d", "b".
e) Set bytes 12-15 to 4 bytes of random data, which will be ignored.
f) Encrypt the 16-byte block using AES-256 in ECB mode with an initialization vector of zero, using the file
encryption key as the key. The result (16 bytes) is stored as the Perms string, and checked for validity
when the file is opened.

#### 7.6.4.4.10  Algorithm 11: Authenticating the user password (Security handlers of revision 6)

a) Test the password against the user key by computing the 32-byte hash using 7.6.4.3.4, "Algorithm 2.B:
Computing a hash (revision 6 and later)" with an input string consisting of the UTF-8 password
concatenated with the 8 bytes of User Validation Salt (see 7.6.4.4.7, "Algorithm 8: Computing the
encryption dictionary’s U (user password) and UE (user encryption) values (Security handlers of
revision 6)"). If the 32- byte result matches the first 32 bytes of the U string, this is the user password.

#### 7.6.4.4.11  Algorithm 12: Authenticating the owner password (Security handlers of revision 6)

a) Test the password against the owner key by computing the 32-byte hash using algorithm 2.B with an
input string consisting of the UTF-8 password concatenated with the 8 bytes of Owner Validation Salt
and the 48 byte U string. If the 32 byte result matches the first 32 bytes of the O string, this is the owner
password.

#### 7.6.4.4.12  Algorithm 13: Validating the permissions (Security handlers of revision 6)

a) Decrypt the 16 byte Perms string using AES-256 in ECB mode with an initialization vector of zero and
the file encryption key as the key. Verify that bytes 9-11 of the result are the characters "a", "d", "b". Bytes
0-3 of the decrypted Perms entry, treated as a little-endian integer, are the user permissions. They
should match the value in the P key. Byte 8 should match the ASCII character "T" or "F" according to the
boolean value of the EncryptMetadata key.

#### 7.6.4.3.4  Algorithm 2.B: Computing a hash (revision 6 and later)

Take the SHA-256 hash of the original input to the algorithm and name the resulting 32 bytes, K.
Perform the following steps (a)-(d) 64 times:

a) Make a new string, K1, consisting of 64 repetitions of the sequence: input password, K, the 48-byte user
key. The 48 byte user key is only used when checking the owner password or creating the owner key. If
checking the user password or creating the user key, K1 is the concatenation of the input password and K.

b) Encrypt K1 with the AES-128 (CBC, no padding) algorithm, using the first 16 bytes of K as the key and
the second 16 bytes of K as the initialization vector. The result of this encryption is E.

c) Taking the first 16 bytes of E as an unsigned big-endian integer, compute the remainder, modulo 3. If the
result is 0, the next hash used is SHA-256, if the result is 1, the next hash used is SHA-384, if the result is
2, the next hash used is SHA-512.

d) Using the hash algorithm determined in step c, take the hash of E. The result is a new value of K, which
will be 32, 48, or 64 bytes in length.
Repeat the process (a-d) with this new value for K. Following 64 rounds (round number 0 to round
number 63), do the following, starting with round number 64:
NOTE 2 The reason for multiple rounds is to defeat the possibility of running all paths in parallel. With 64
rounds (minimum) there are 3^64 paths through the algorithm.

e) Look at the very last byte of E. If the value of that byte (taken as an unsigned integer) is greater than the
round number - 32, repeat steps (a-d) again.

f) Repeat from steps (a-e) until the value of the last byte is ≤ (round number) - 32.
NOTE 3 Tests indicate that the total number of rounds will most likely be between 65 and 80.
The first 32 bytes of the final K are the output of the algorithm.
